define(
    [
        'Backbone'
    ],
    function(Backbone)
    {
        var valueModel = Backbone.Model.extend(
            {
                
            });

            return valueModel;
    });